@using InventoryManagement.Repository
@model IEnumerable<ExpenseAllViewModel>
@{
    ViewBag.Title = "Expense";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="m-3">
    <h4 class="page-header mb-3">Expense</h4>

    <div class="card card-body">
        <div class="table-responsive">
            <table id="data-table" class="table table-sm table-bordered">
                <thead>
                    <tr>
                        <th>Voucher No</th>
                        <th>Category</th>
                        <th>Amount</th>
                        <th>Expense&nbsp;For</th>
                        <th>Expense&nbsp;Date</th>
                        <th>Posted By</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.VoucherNo</td>
                            <td>@item.ExpenseCategory</td>
                            <td>@item.ExpenseAmount</td>
                            <td>@item.ExpenseFor</td>
                            <td>@item.ExpenseDate.ToString("d MMM yyyy")</td>
                            <td>@item.CreateBy</td>
                            <td>
                                @if (item.IsTransportation)
                                {
                                    <a href="/Expenses/TransportationCostDetails/@item.Id">Details</a>
                                }
                                else
                                {
                                    <a href="/Expenses/GeneralExpenseDetails/@item.Id">Details</a>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


@section Scripts {
    <script>
        $(function () {
            $("#data-table").DataTable();
        });

            //const dataTable = {
            //    table: null,
            //    filter: null,
            //    init: function() {
            //        dataTable.table = $("#data-table1").DataTable({
            //            processing: true,
            //            serverSide: true,
            //            ajax: {
            //                url: "/Expenses/GetExpense",
            //                type: "POST"
            //            },
            //            columns:
            //            [
            //                { data: "VoucherNo" },
            //                { data: "ExpenseCategory" },
            //                { data: "ExpenseAmount" },
            //                { data: "ExpenseFor" },
            //                { data: "ExpenseDate", "render": function(data) { return moment(data).format('DD MMM YYYY') } },
            //                { data: "Id", "render": function(data) { return `<a class="fas fa-edit update" href="/Expense/Edit/${data}"></a>`; } }
            //            ],
            //            columnDefs: [
            //                { 'searchable': false, 'targets': [4] },
            //                { 'sortable': false, 'targets': [4] },
            //                { 'className': "text-left", "targets": [0, 2] }
            //            ]
            //        });
            //    },
            //    getData: function() {
            //        dataTable.table ? dataTable.table.ajax.reload() : dataTable.init();
            //    }
            //}
    </script>
}
